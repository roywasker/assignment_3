#ifndef _MY_MAT_H_
#define _MY_MAT_H_

/*
The function will receive a pointer to the array and the number of the function will move the following i elements in the array one cell to the right.
*/
void shift_element(int* arr, int i);

/*
The function receive an array of size len and sort it using the insertion sort algorithm.
*/
void insertion_sort(int* arr , int len);

#endif